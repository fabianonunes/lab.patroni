services:
  patroni:
    override: replace
    startup: enabled
    user: postgres
    group: postgres
    command: patroni /etc/patroni/patroni.yaml

  cron:
    override: replace
    startup: enabled
    user: postgres
    group: postgres
    command: supercronic -passthrough-logs /etc/crontab
